spring:
  application:
    name: truco

truco:
  match:
    games-to-win: 3
    points-to-win-game: 3
  security:
    jwt-secret: ${TRUCO_JWT_SECRET}
    issuer: ${TRUCO_JWT_ISSUER:truco-api}
    audience: ${TRUCO_JWT_AUDIENCE:truco-clients}
    access-token-expiration-seconds: 86400
